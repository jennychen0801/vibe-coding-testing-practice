---
description: DashboardPage 測試案例
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限

---

## [ ] 【前端元素】渲染儀表板基本元素
**範例輸入**：無
**期待輸出**：畫面應包含「儀表板」標題、無頭像時顯示 `?` 或用戶名稱開頭字母、歡迎標語、「登出」按鈕，以及「商品列表」區塊

---

## [ ] 【前端元素】僅 admin 角色可見管理後台連結
**範例輸入**：`user.role` 分別為 `'admin'` 和 `'user'`
**期待輸出**：為 `'admin'` 時顯示「🛠️ 管理後台」連結；為 `'user'` 時不顯示該連結

---

## [ ] 【function 邏輯】登出功能正常運作
**範例輸入**：點擊「登出」按鈕
**期待輸出**：呼叫 `logout()` 函數，並導向至 `/login` 且帶有 `replace: true` 與 `state: null`

---

## [ ] 【Mock API】顯示商品載入中狀態
**範例輸入**：`productApi.getProducts()` 尚未回傳
**期待輸出**：商品列表區塊顯示「載入商品中...」與載入動畫

---

## [ ] 【Mock API】成功取得商品列表並正確渲染
**範例輸入**：API 回傳包含數個商品的陣列（例如名稱、描述、價格為 1200）
**期待輸出**：畫面正確渲染商品卡片，且價格顯示格式化（如 `NT$ 1,200`）

---

## [ ] 【Mock API】API 發生一般錯誤時顯示錯誤訊息
**範例輸入**：API 回傳 `400` 錯誤，訊息為「無法載入商品資料」
**期待輸出**：畫面顯示錯誤圖示與「無法載入商品資料」字樣

---

## [ ] 【Mock API】API 發生 401 錯誤時由 interceptor 處理
**範例輸入**：API 回傳 `401` 錯誤
**期待輸出**：不應顯示錯誤訊息卡片（直接 return，交由 axios interceptor 處理）
