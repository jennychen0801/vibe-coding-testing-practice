---
description: AdminPage 測試案例
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限

---

## [ ] 【前端元素】渲染管理後台基本元素
**範例輸入**：無
**期待輸出**：畫面應包含「返回」連結、「管理後台」標題、「登出」按鈕，以及「管理員專屬頁面」的各項特色說明

---

## [ ] 【前端元素】根據 admin 角色顯示標籤
**範例輸入**：`user.role` 為 `'admin'`
**期待輸出**：標籤顯示「管理員」

---

## [ ] 【前端元素】根據 user 角色顯示標籤
**範例輸入**：`user.role` 為 `'user'`
**期待輸出**：標籤顯示「一般用戶」

---

## [ ] 【function 邏輯】點擊返回導向
**範例輸入**：點擊「返回」連結
**期待輸出**：應導向至 `/dashboard`

---

## [ ] 【function 邏輯】登出功能正常運作
**範例輸入**：點擊「登出」按鈕
**期待輸出**：呼叫 `logout()` 函數，並導向至 `/login` 且帶有 `replace: true` 與 `state: null`
